# CLAUDE.md

## 주석 규칙

주석은 기본적으로 쓰지 않는다. 아래 두 가지만 허용한다.

- 코드를 이해하는 데 꼭 필요한 내용 (코드만으로는 알 수 없는 외부 제약 등)
- 카테고리(섹션) 분리용 헤더 주석

쓰지 않는다:

- 변경 내역 / 히스토리 (`// v2에서 추가`, `// 기존에는 ~였음`)
- 코드 자체로 알 수 있는 내용 (`// 배열 초기화`)
- 코드 의도 설명 (왜 이렇게 했는지에 대한 서술)
- 파일 상단 배너, 함수 설명 등 문서화 목적의 주석

가능한 한 모든 주석을 제거한다. 린터 지시자(`/* global ... */`, `// eslint-disable-next-line`)는 주석이 아니라 코드로 취급해 유지한다.

섹션 헤더는 짧은 명사구로 쓴다. 설명을 덧붙이지 않는다.

```css
/* ---- Feeds ------------------------------------------------------------- */
```
